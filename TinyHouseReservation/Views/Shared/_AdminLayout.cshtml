@*
    Bu dosya admin sayfaları için özel bir layout olarak kullanılacak.
*@
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Panel - @ViewData["Title"]</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

    <style>
        body {
            background-color: #f8f9fa;
        }

        footer {
            background-color: #f1f3f5;
            padding: 15px 0;
        }

        .container {
            background: white;
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgb(0 0 0 / 0.1);
            margin-bottom: 40px;
        }

        .navbar-nav .nav-link.active {
            font-weight: 600;
            color: #0d6efd !important;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="#">Admin Panel</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar" aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="adminNavbar">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link @((ViewContext.RouteData.Values["action"]?.ToString() == "Dashboard") ? "active" : "")" href="/Admin/Dashboard">Dashboard</a>
                    </li>
                    @*
                    <li class="nav-item">
                        <a class="nav-link @((ViewContext.RouteData.Values["controller"]?.ToString() == "Admin" && ViewContext.RouteData.Values["action"]?.ToString().StartsWith("Kullanici") == true) ? "active" : "")" href="/Admin/Kullanicilar">Kullanıcılar</a>
                    </li>/li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Ev/Ilanlar">İlanları Listele</a>
                        *@
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Admin/Rezervasyonlar">Rezervasyonlar</a>
                    <
                    <li class="nav-item">
                        <a class="nav-link" href="/Admin/Ilanlar">İlanlar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Admin/Odemeler">Ödeme Raporları</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Admin/Istatistik">📊 İstatistikler</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Admin/KullanicilariListele">Kullanıcı Listesi</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Admin/KullaniciEkle">Kullanıcı Ekle</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/">Siteye Dön</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        @RenderBody()
    </div>

    <footer class="text-center text-muted">
        <hr />
        <p>&copy; 2025 - TinyHouseReservation Admin</p>
    </footer>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
